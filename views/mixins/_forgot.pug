mixin forgotForm()
  form.form(action="/account/forgot" method="POST" style="margin-top:20px")
    h2 I forgot my password!
    label(for="email") Email
    input(type="email" name="email")
    input.button(type="submit" value="Send a Reset")
